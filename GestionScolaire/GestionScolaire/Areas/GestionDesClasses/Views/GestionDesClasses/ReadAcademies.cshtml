
@using GestionScolaire.Models
@model IEnumerable<AcademieModels>

@{
    ViewBag.Title = "Index";
}

<h2>Ensemble des academies</h2>

<p>
    @Html.ActionLink("Creation d'une nouvelle academie", "CreateAcademie")
</p>
<table>
    <tr>
        <th>
            Titre
        </th>
    </tr>

@foreach (AcademieModels academ in Model) {
    <tr>
        <td>
            @academ.name
        </td>
        <td>
            @Html.ActionLink("Edit", "EditAcademie", new { id = academ.id }, null) |
            @Html.ActionLink("Details", "ReadAcademie", new { id = academ.id }) |
            @Html.ActionLink("Delete", "DeleteAcademie", new { id = academ.id })
        </td>
    </tr>
}

</table>