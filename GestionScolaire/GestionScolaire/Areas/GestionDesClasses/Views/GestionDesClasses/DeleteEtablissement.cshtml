@using GestionScolaire.Areas.GestionDesClasses.Models
@model EtablissementModels

@{
    ViewBag.Title = "Delete";
}

<h2>Suppresion de  @Model.name</h2>

@Html.ValidationSummary(true)

@{Html.BeginForm("DeleteEtablissement", "GestionDesClasses", FormMethod.Post);}
@Html.HiddenFor(model => model.id)
<h3>Voulez-vous vraiment supprimer cet élément???</h3>
<table class="table">
    <tr>
        <td>
            <button>Oui</button>
            <button type="button">@Html.ActionLink("Non", "ReadEtablissements", "GestionDesClasses")</button>
        </td>
    </tr>
</table>
@{Html.EndForm();}
